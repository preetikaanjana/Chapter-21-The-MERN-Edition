# GitHub Profile Inspector

Look up GitHub profiles and save notes

## Run

```bash
# Backend (port 5000)
cd backend && npm install && npm run dev

# Frontend (port 5173)
cd frontend && npm install && npm run dev
```

Requires MongoDB running locally.
